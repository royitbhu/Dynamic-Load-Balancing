Global setup:

 Set up git
  git config --global user.name "Rahul jain"
  git config --global user.email rahuljain.roy@gmail.com
      

Next steps:

  mkdir Dynamic-Load-Balancing
  cd Dynamic-Load-Balancing
  git init
  touch README
  git add README
  git commit -m 'first commit'
  git remote add origin git@github.com:the-codebreaker/Dynamic-Load-Balancing.git
  git push -u origin master
      

Existing Git Repo?

  cd existing_git_repo
  git remote add origin git@github.com:the-codebreaker/Dynamic-Load-Balancing.git
  git push -u origin master
      

Importing a Subversion Repo?

  Check out the guide for step by step instructions.
      

When you're done:

  Continue


